#include <stdio.h>
/**
 * main - print the alphabet in lowetcase.
 * Return: Always 0.
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)

		putchar(letter);
	putchat('\n');

	return (0);
}
